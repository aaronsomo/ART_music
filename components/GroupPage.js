import React, { useState, useEffect, useCallback } from 'react';
import {
  StyleSheet,
  Text,
  View,
  Button,
  Image,
  FlatList,
  SafeAreaView,
  Dimensions,
  ScrollView,
  TextInput,
  Modal,
  Input,
  TouchableHighlight
} from 'react-native';

const GroupPage = props => {
  return (
    <View>
      <Text>INDIVIDUAL GROUP PAGE</Text>
    </View>
  );
};

export default GroupPage;
